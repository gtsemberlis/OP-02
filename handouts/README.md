# Player Handouts

This directory contains canonical, player-facing in-world documents.

- `Player Version` is the exact prop content that may be distributed to players.
- `GM Annotation` is Handler-only publication material.
- Operational custody, discovery, and access remain defined by the related object in `artifacts/`.
- Handout references use `H-##`; Artifact references use `A-##`.
